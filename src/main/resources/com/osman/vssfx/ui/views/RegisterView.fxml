<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<AnchorPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.osman.vssfx.ui.controllers.RegisterController">

    <children>
        <StackPane prefWidth="1100.0" prefHeight="700.0"
                   AnchorPane.topAnchor="0.0" AnchorPane.bottomAnchor="0.0"
                   AnchorPane.leftAnchor="0.0" AnchorPane.rightAnchor="0.0">

            <VBox spacing="14.0" maxWidth="460.0"
                  style="-fx-background-radius: 16; -fx-padding: 22;">
                <padding><Insets top="22" right="22" bottom="22" left="22"/></padding>

                <Label text="Hesap Oluştur">
                    <font><Font size="24"/></font>
                </Label>

                <Label text="Firma ve yönetici hesabını oluşturarak sisteme başlayın."
                       wrapText="true" style="-fx-opacity: 0.85;"/>

                <Separator />

                <VBox spacing="8">
                    <Label text="Firma Adı" />
                    <TextField fx:id="companyField" promptText="Örn: Osman Oto Servis" />

                    <Label text="Ad Soyad" />
                    <TextField fx:id="fullNameField" promptText="Örn: Osman Korkmaz" />

                    <Label text="E-posta" />
                    <TextField fx:id="emailField" promptText="ornek@domain.com" />

                    <Label text="Şifre" />
                    <HBox spacing="10" alignment="CENTER_LEFT">
                        <StackPane HBox.hgrow="ALWAYS">
                            <PasswordField fx:id="passwordField" promptText="Şifre"/>
                            <TextField fx:id="passwordVisibleField" promptText="Şifre"
                                       visible="false" managed="false"/>
                        </StackPane>

                        <ToggleButton fx:id="showPasswordBtn" text="👁" focusTraversable="false"
                                      onAction="#onTogglePassword"
                                      style="-fx-background-radius: 10; -fx-min-width: 46; -fx-min-height: 36;"/>
                    </HBox>

                    <Label text="Şifre (Tekrar)" />
                    <PasswordField fx:id="password2Field" promptText="Şifre (Tekrar)"/>
                </VBox>

                <Button fx:id="registerBtn" maxWidth="1.7976931348623157E308" onAction="#onRegister">
                    <graphic>
                        <HBox spacing="10" alignment="CENTER">
                            <ProgressIndicator fx:id="registerSpinner"
                                               visible="false" managed="false"
                                               prefWidth="16" prefHeight="16"/>
                            <Label fx:id="registerBtnText" text="Kayıt Ol"/>
                        </HBox>
                    </graphic>
                </Button>

                <HBox alignment="CENTER" spacing="8">
                    <Button text="Giriş ekranına dön" onAction="#onBackToLogin" styleClass="ghost"/>
                </HBox>

                <Label fx:id="msgLabel" text="" wrapText="true" style="-fx-text-fill: #d64545;" />
                <Label fx:id="okLabel" text="" wrapText="true" style="-fx-text-fill: #2b8a3e;" />
            </VBox>
        </StackPane>
    </children>
</AnchorPane>
